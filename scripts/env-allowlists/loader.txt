# loader (ROLE=loader: 03:30 transform raw_tdx -> this env's PG_SCHEMA)
# allowlist. Source of truth: grep os.Getenv in services/functions/
# loader_cron.go, services/functions/loader.go, services/shared/bootstrap.go.
# No TDX_CLIENT_ID/TDX_CLIENT_SECRET here — the loader never calls TDX, only
# reads already-landed raw_tdx rows. See scripts/check-env-allowlist.sh and
# docs/config.md.
DATABASE_URL
PG_SCHEMA
# changetovector runs in the loader (registerLoaderCrons -> runVectorRefresh),
# so the loader needs the embedding endpoint. Empty EMBED_URL => embedding is
# skipped (changeToVector no-ops) but the pipeline marker is still written.
EMBED_URL
LOAD_DB_MAX_CONNS
LOAD_DB_MIN_CONNS
RAW_DATABASE_URL
RAW_DB_MAX_CONNS
LOAD_ON_BOOT
LOAD_QUARANTINE_MAX_RATIO
PG_STATEMENT_TIMEOUT
REDIS_PASSWORD
HEALTH_FILE
SENTRY_DSN
SENTRY_ENVIRONMENT
SENTRY_TRACES_SAMPLE_RATE
