services:
  router:
    environment:
      APP_ENV: staging
      PG_SCHEMA: staging
      ROUTER_DB_MAX_CONNS: 20
      ROUTER_DB_MIN_CONNS: 2

  functions:
    environment:
      APP_ENV: staging
      PG_SCHEMA: staging
      FUNCTIONS_DB_MAX_CONNS: 10
      FUNCTIONS_DB_MIN_CONNS: 2

  ingestor:
    environment:
      APP_ENV: staging
      PG_SCHEMA: staging
      FUNCTIONS_DB_MAX_CONNS: 10
      FUNCTIONS_DB_MIN_CONNS: 2

  loader:
    environment:
      APP_ENV: staging
      PG_SCHEMA: staging

  powersync:
    environment:
      APP_ENV: staging
      PG_SCHEMA: staging
