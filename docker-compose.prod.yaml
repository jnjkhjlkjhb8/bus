services:
  router:
    environment:
      APP_ENV: production
      PG_SCHEMA: public
      ROUTER_DB_MAX_CONNS: 20
      ROUTER_DB_MIN_CONNS: 2

  functions:
    environment:
      APP_ENV: production
      PG_SCHEMA: public
      FUNCTIONS_DB_MAX_CONNS: 10
      FUNCTIONS_DB_MIN_CONNS: 2

  ingestor:
    environment:
      APP_ENV: production
      PG_SCHEMA: public
      FUNCTIONS_DB_MAX_CONNS: 10
      FUNCTIONS_DB_MIN_CONNS: 2

  powersync:
    environment:
      APP_ENV: production
      PG_SCHEMA: public
